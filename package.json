{
  "name": "review-master",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Turn pull-request reviews into XP. Stake. Distribute. Climb the board.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/patch-manifest.mjs",
    "types:db": "supabase gen types typescript --project-id $SUPABASE_PROJECT_ID --schema public > src/types/database.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@crxjs/vite-plugin": "^2.0.0-beta.28",
    "@types/chrome": "^0.0.270",
    "@types/webextension-polyfill": "^0.12.1",
    "supabase": "^1.200.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
